const vscode = require('vscode');

/**
 * Registers go to definition for Nuvsha.
 * @param {vscode.ExtensionContext} context
 */
function registerDefinition(context) {
    // TODO: Implement Nuvsha go to definition
    // Will understand component imports and allow navigation
}

module.exports = {
    registerDefinition
};
