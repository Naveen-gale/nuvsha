const vscode = require('vscode');

/**
 * Registers the autocomplete provider for Nuvsha.
 * @param {vscode.ExtensionContext} context
 */
function registerCompletion(context) {
    // TODO: Implement Nuvsha autocomplete logic
    // Will provide completion items for tags, attributes, and variables
}

module.exports = {
    registerCompletion
};
