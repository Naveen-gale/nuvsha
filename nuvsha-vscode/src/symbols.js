const vscode = require('vscode');

/**
 * Registers document symbols for Nuvsha.
 * @param {vscode.ExtensionContext} context
 */
function registerSymbols(context) {
    // TODO: Implement Nuvsha document symbols
    // Will provide document outline and component/variable discovery
}

module.exports = {
    registerSymbols
};
