<script>
  import Button from "../components/Button.nuv";
  import { navigate } from "nuvsha";

  goHome = () => {
    navigate("/")
  }
</script>

<div class="min-h-[50vh] flex flex-col items-center justify-center text-center p-8">
  <div class="w-16 h-16 rounded-2xl bg-rose-500/10 border border-rose-500/25 flex items-center justify-center text-3xl mb-4">
    🧭
  </div>
  <h1 class="text-3xl font-black text-white mb-2 tracking-tight">404 — Route Not Found</h1>
  <p class="text-slate-400 text-sm max-w-md mb-6 leading-relaxed">
    The Nuvsha SPA router successfully intercepted the unknown route and resolved the fallback wildcard route handler (<code class="text-pink-400 font-mono">*</code>).
  </p>
  <Button onclick={() => goHome()}>
    Return to Master Test Overview
  </Button>
</div>
