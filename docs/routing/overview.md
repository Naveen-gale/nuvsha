# Routing Overview

Nuvsha comes with a built-in Single Page Application (SPA) client-side router. 

When a user navigates around a Nuvsha application, the page doesn't actually reload. Instead, the router intercepts the navigation, updates the URL, and dynamically swaps out the current screen with the new one. This results in lightning-fast, native-feeling applications.
