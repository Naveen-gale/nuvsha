# Architecture Overview

As your Nuvsha application grows, placing all your logic and HTML inside `App.nuv` becomes unmanageable.

A well-architected Nuvsha application strictly separates concerns into specific folders, matching the default structure provided by `create-nuvsha`.
