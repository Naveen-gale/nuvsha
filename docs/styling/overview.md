# Styling Overview

Nuvsha does not invent a new way to write CSS. Because Nuvsha embraces standard web technologies, you can style your application using any standard CSS approach you prefer.

The two most common approaches are:
1. Normal CSS files imported via Vite.
2. Tailwind CSS utility classes.
